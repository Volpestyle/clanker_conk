import { test } from "bun:test";
import assert from "node:assert/strict";

test("replyFollowup: legacy followup loop tests removed (replaced by native tool call loop in bot.ts)", () => {
  assert.ok(true);
});
